<types namespace="_rqt">
  <type record name="SessionOptions" desc="Options for a session.">
    <prop string opt name="host">
      The prefix to each request, such as `https://rqt.biz`.
    </prop>
    <prop opt type="!http.OutgoingHttpHeaders" name="headers">
      Headers to use for each request.
    </prop>
  </type>
</types>